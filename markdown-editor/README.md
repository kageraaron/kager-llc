# Markdown Editor & Previewer

A simple, fast, dual-pane Markdown editor with a live HTML preview.

## Features
- Live parsing using `marked.js`.
- Clean, responsive dual-pane interface.
- 100% client-side rendering for complete privacy.

## Setup
No build steps required. Open `index.html` in a browser or deploy to a static host like Vercel.
