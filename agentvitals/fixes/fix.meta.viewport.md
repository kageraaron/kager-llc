# AgentVitals fix: Add a viewport meta tag

**Failure class:** `fix.meta.viewport`  ·  **Severity:** low  ·  **Occurrences:** 19

## Why agents get stuck
Makes rendering predictable for mobile and headless-agent viewports.

## Where (sampled)
- `page <head>`

## Suggested change
```
<meta name="viewport" content="width=device-width, initial-scale=1" />
```

_Generated by AgentVitals. Review, apply in your codebase, and the CI gate will confirm the regression is fixed._