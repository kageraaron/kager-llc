# Password Generator

A secure, client-side tool to generate strong random passwords.

## Features
- Cryptographically secure random number generation using `window.crypto`.
- Customizable length and character sets.
- 100% client-side: passwords are never sent to a server.

## Setup
No build steps required. Open `index.html` in a browser or deploy to a static host like Vercel.
